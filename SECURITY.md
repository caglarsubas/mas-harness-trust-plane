# Security policy

Report vulnerabilities privately through GitHub's security advisory interface.
Do not include live tokens, tenant data, credentials, private keys, policy
source, or production endpoints in a report. The foundation fails closed when
identity, policy, decision, cache, or audit dependencies are unavailable.
